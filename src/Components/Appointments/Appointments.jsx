import React from 'react';
import Calendar from '../Calendar/Calendar';

const Appointments = () => {
    return (
        <div>
            <Calendar  userType = 'global'/>
        </div>
    );
};

export default Appointments;